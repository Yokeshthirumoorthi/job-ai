[project]
name = "job-ai"
version = "0.1.0"
description = "Typed multi-board job scraper built on JobSpy"
requires-python = ">=3.10"
dependencies = [
    "python-jobspy>=1.1.80",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "pyyaml>=6.0",
    "openai>=1.40",
]

[tool.uv]
package = false
